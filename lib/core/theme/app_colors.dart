import 'package:flutter/material.dart';

//hala düzenlenmeli, kullanılmıyor
class AppColors {
  // Ana renk paleti
  static const Color primary = Color(0xFF3F8CFF);
  static const Color secondary = Color(0xFF1E1E1E);

  // Arka plan renkleri
  static const Color background = Color(0xFFF7F8FA);
  static const Color cardBackground = Colors.white;

  // Metin renkleri
  static const Color textDark = Color(0xFF2E2E2E);
  static const Color textMedium = Color(0xFF555555);
  static const Color textLight = Color(0xFF9B9B9B);

  // Durum renkleri
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFC107);

  // Sınır ve gölge renkleri
  static const Color border = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x11000000); // %7 opacity

  // UI bileşen renkleri
  static const Color tabInactive = Color(0xFFE6E6E6);
  static const Color tabActive = primary;

  // Arama çubuğu arka plan
  static const Color searchBackground = Color(0xFFF1F3F5);

  // Buton renkleri
  static const Color buttonPrimary = primary;
  static const Color buttonText = Colors.white;
}
