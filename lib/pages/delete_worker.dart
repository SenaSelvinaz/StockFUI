import 'package:flutter/material.dart';

class DeleteWorker extends StatelessWidget {
  const DeleteWorker({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kayıt Sil")),
      body: const Center(child: Text("Silme ekranı buraya gelecek")),
    );
  }
}
